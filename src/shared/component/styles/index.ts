import { dialogTheme } from "./component/Dialog";
import { selectTheme } from "./component/Select";
import { drawerTheme } from "./component/Drawer";

const customTheme = {
  dialog: dialogTheme,
  select: selectTheme,
  drawer: drawerTheme,
};

export default customTheme;
